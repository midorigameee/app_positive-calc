<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <script>
    'use strict';
      var local = window.location;
      var url = local.origin;
      url + getDir(local); // 現在のディレクトリ
      console.log("Current Dir : " + url);
      url + getDir(local,1); // 1つ上のディレクトリ
      console.log("One higher Dir : " + url);
      function getDir(place, n) {
      　　return place.pathname.replace(new RegExp("(?:\\\/+[^\\\/]*){0," + ((n || 0) + 1) + "}$"), "/");
      }
    </script>
    <link rel='stylesheet' href='./stylesheets/style.css'>
    <link rel='stylesheet' href='./stylesheets/textmoving.css'>
    <script src="./javascripts/textmoving.js"></script>
  </head>
  <body>
    <div class="f-container">
      <!-- 上段の3個 -->
      <div class="f-item f-item-left">
        <% if(judgement) { %>
          <p class="sabu-word">脳の回転速すぎて火が出てる！</p>
        <% } %>
      </div>
      <div class="f-item f-item-center">
        <% if(judgement) { %>
          <p class="sabu-word">現代のアインシュタイン！</p>
        <% } %>
      </div>
      <div class="f-item f-item-right">
        <% if(judgement) { %>
          <p class="sabu-word">脳みその皺がラビリンス！</p>
        <% } %>
      </div>

      <!-- 中段の左 -->
      <div class="f-item f-item-left">
        <% if(judgement) { %>
          <p class="sabu-word">ベガバンクもびっくり！</p>
        <% } %>
      </div>

      <div class="f-item f-item-center">
        <h1><%= title %></h1>
        <% if(judgement) { %>
          <p class="text">
            <span>正解！</span><strong>天才</strong><span>です！！</span><br>
          </p>
        <% } else { %>
          <p class="text">
            <span>伸びしろは無限大…！</span>
          </p>
        <% } %>
        <a href="/">もう一度</a>
      </div>

      <!-- 中段の右 -->
      <div class="f-item f-item-right">
        <% if(judgement) { %>
          <p class="sabu-word">人間国宝！ルーブル美術館に展示したい！</p>
        <% } %>
      </div>

      <!-- 下段の3個 -->
      <div class="f-item f-item-left">
        <% if(judgement) { %>
          <p class="sabu-word">脳みそ詰まりすぎ、密です！</p>
        <% } %>
      </div>
      <div class="f-item f-item-center">
        <% if(judgement) { %>
          <p class="sabu-word">脳みそは密でいいんだよ！</p>
        <% } %>
      </div>
      <div class="f-item f-item-right">
        <% if(judgement) { %>
          <p class="sabu-word">羂索に見せつけてやれ！！</p>
        <% } %>
      </div>
    </div>
  </body>
</html>
